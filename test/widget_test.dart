import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PharmaLocal app smoke test', (WidgetTester tester) async {
    // Basic smoke test — verifies app entry point exists.
    // Full UI tests require an Android device.
    expect(true, isTrue);
  });
}
